source 'https://rubygems.org'

# Core dependencies
gem 'rake'
gem 'concurrent-ruby'
gem 'connection_pool'
gem 'httparty'
gem 'faker'
gem 'get_process_mem'
gem 'json'

# JRuby specific gems
platform :jruby do
  gem 'jruby-openssl'
end

# Development dependencies
group :development do
  gem 'pry'
  gem 'rubocop'
  gem 'yard'
end

# Test dependencies
group :test do
  gem 'rspec'
  gem 'benchmark-ips'
  gem 'minitest'
end